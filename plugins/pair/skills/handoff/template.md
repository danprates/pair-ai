# Handoff: <title>

## What was done

- (Summarize what changed in plain language, without technical jargon. Write for someone who did not follow the development.)

## Scenarios covered

- (List each business scenario or use case this change handles. One bullet per scenario. Be specific: "when the user does X, Y happens now.")

## How to integrate

- (Explain what a frontend developer needs to know to consume or use this change: new endpoints, changed contracts, new fields, removed fields, required headers, etc.)
- (If nothing changed from the frontend's perspective, write: No integration changes required.)

## How to implement

Step-by-step for a frontend developer or QA to use or validate this change:

1. (First step — e.g. call endpoint `POST /api/foo` with body `{ ... }`)
2. (What to expect in the response or UI)
3. (Any edge case or variation worth verifying)
